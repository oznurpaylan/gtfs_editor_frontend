export interface FareAttribute {
  fare_id: string,
  price: string,
  currency_type: string,
  payment_method: string,
  transfers: string,
  agency_id: string,
  transfer_duration: string,
  session_id: string
}
